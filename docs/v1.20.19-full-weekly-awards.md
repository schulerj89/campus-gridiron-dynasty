# v1.20.19 Full Weekly Awards

## Expert Finding

The code-review expert found that `createWeeklyAwards` creates four national weekly awards, but the Awards page sliced that list to two cards. The dashboard should stay compact, but the dedicated Awards page should expose the full weekly result.

## Decision

Treat this as a patch-level UI improvement because it displays already-generated award data without changing award calculations, storage, or weekly simulation.

## Fix

- Changed the Awards page national weekly panel to render up to four weekly awards.
- Left the dashboard `Latest National Awards` slice unchanged.
- Added smoke assertions for `Ground Surge` and `Sky Route`.

## Validation

- Regenerated `artifacts/screenshots/player-of-week-desktop.png`.
- Ran `npm run build`, `npm run test`, `npm run release:check`, and `npm run smoke`.
